{{ form_start(form, {'attr': {'class': '',
						 'novalidate': 'novalidate',
                         'enctype': 'multipart/form-data'
						} 
}) }}

<div class="container-fluid m-0">
	<div class="row m-1">
		<div class="col">
			<div class="form-row">
				<div class="col-lg-3 form-group">
					<span class="text-danger">{{ form_errors(form.file) }}</span>
					{{ form_label(form.file, null, {
							'label_attr': {'class': 'form-label form-label-sm'}
					}) }}
					{{ form_widget(form.file, {'attr': {'class': 'col-sm-10 form-control-file form-control-sm'}}) }}
				</div>
{#				<div class="col-lg-2 form-group">
					{{ form_label(form.receiptsType) }}
					{{ form_widget(form.receiptsType, {'attr': {'class': 'js-receiptsType form-control form-control-sm'}}) }}
				</div>
				<div class="col-lg-2 form-group">
					{{ form_label(form.receiptsFinishStatus) }}
					{{ form_widget(form.receiptsFinishStatus, {'attr': {'class': 'js-receiptsFinishStatus form-control form-control-sm'}}) }}
				</div>
#}			</div>
		</div>
	</div>
</div>
			
<div class="row justify-content-center mt-2">
	<button class="btn btn-primary ml-2 js-save" type="submit">{{ "btn.send" | trans }}</button>
	<button class="btn btn-primary ml-2 js-back" type="button" data-url="{{ path('receipts_file_list') }}">{{ "btn.cancel" | trans }}</button>
</div>

<!-- Token CSRF -->
{{ form_widget(form._token) }}
{{ form_end(form, {'render_rest': false, 
				   'attr': {'class': '',
			       } 
}) }}