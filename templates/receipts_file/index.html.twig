{% extends 'base.html.twig' %}

{% block title %}List Receipts Files{% endblock %}

{% block body %}
    <main class="container-fluid" >

		<div class="container-fluid">
			{{ include('receipts_file/_list.html.twig') }}
			<span class="pull-right"><a href="{{ path ('receipts_file_upload') }}" class="btn btn-primary rounded-circle fixed-bottom-right"><i class="fa fa-plus" title='{{ "btn.new" | trans | capitalize }}'></i></a></span>
		</div>
    </main>
{% endblock %}

{# {% block stylesheets %}
	{{ parent() }}
	{{ encore_entry_link_tags('receiptsFile_list') }}
{% endblock %}

{% block javascripts %}
	{{ parent() }}
	{{ encore_entry_script_tags('receiptsFile_list') }}
{% endblock %} #}